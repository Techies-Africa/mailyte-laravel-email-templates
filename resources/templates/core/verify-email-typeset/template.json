{
    "slug": "verify-email-typeset",
    "name": "Verify email address",
    "description": "Sent immediately after sign-up to confirm the address is real and reachable. Typographic treatment on warm paper.",
    "version": "1.0.0",
    "engine": "^1.0",
    "tier": "core",
    "variant_of": "verify-email",
    "variant_label": "Typeset",
    "type": "transactional",
    "category": "account",
    "supported_layouts": [
        "plain",
        "minimal",
        "branded"
    ],
    "audience": [
        "saas",
        "professional-services"
    ],
    "tone": "neutral",
    "tags": [
        "verification",
        "account",
        "security",
        "signup"
    ],
    "authors": [
        {
            "name": "Confidence Ugolo",
            "url": "https://www.linkedin.com/in/confidence-ugolo"
        },
        {
            "name": "Joel Omojefe",
            "url": "https://www.linkedin.com/in/joel-omojefe"
        }
    ],
    "license": "MIT",
    "origin": {
        "kind": "original",
        "note": "Design: typeset — warm paper, serif display, the code as a specimen between rules."
    },
    "subject": "{{ verification_code }} is your {{ product.name }} verification code",
    "preheader": "The code is good for {{ expires_in }} and works once.",
    "blocks_used": [
        "button",
        "code",
        "divider",
        "heading",
        "text"
    ],
    "variables": {
        "user.first_name": {
            "type": "string",
            "description": "Recipient's first name. Falls back to a generic greeting when absent.",
            "default": "",
            "example": "Ada"
        },
        "heading_text": {
            "type": "string",
            "description": "Main headline. Every visible string is a variable, so this is editable without touching markup.",
            "default": "Confirm your email address"
        },
        "body": {
            "type": "text",
            "description": "Opening paragraph.",
            "default": "Enter this code in the window where you started signing up. It only works once."
        },
        "verification_code": {
            "type": "string",
            "description": "The code shown in the panel and in the subject line.",
            "required": true,
            "example": "294 501"
        },
        "expires_in": {
            "type": "string",
            "description": "How long the code stays valid. Stated explicitly because senders genuinely differ here, from five minutes to 24 hours.",
            "default": "10 minutes"
        },
        "action_url": {
            "type": "url",
            "description": "One-click verification link. Omit it to send a code-only email.",
            "default": "",
            "example": "https://example.com/verify/abc123"
        },
        "button_label": {
            "type": "string",
            "description": "Call to action, used only when action_url is set.",
            "default": "Verify email address"
        },
        "security_note": {
            "type": "text",
            "description": "Closing reassurance. Near-universal convention in shipped verification email.",
            "default": "If you didn't create an account, you can safely ignore this email. Nothing happens until the code is used."
        }
    }
}
